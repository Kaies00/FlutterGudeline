import 'package:flutter/material.dart';

class AfterSplashScreenPage extends StatelessWidget {
  const AfterSplashScreenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('The Next Page after Splash screen')),
    );
  }
}